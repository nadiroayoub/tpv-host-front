/**
 * LLamar a este servicio en cualquier momento que hago un request
 * Para controlar (hundle) mas los errores
 * Facil de encontrar el error!
 */
import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class HttpService {

  constructor() { }
}
